# Copy to .env.development.local for local overrides (not committed).

# Production API base is usually set in hosting (Vercel) — see README.

# Optional: Sentry browser SDK (create a React project in Sentry, paste browser DSN)
# REACT_APP_SENTRY_DSN=https://...@....ingest.sentry.io/...
# REACT_APP_SENTRY_RELEASE=campus-link-client@0.1.0
